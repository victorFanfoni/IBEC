<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Define os cantos arredondados como no EditText -->
    <corners android:radius="50dp"/>

    <!-- Cor de fundo cinza com transparência (ajustável) -->
    <solid android:color="#80535353"/> <!-- 50% transparente -->

    <!-- Adicione um efeito de borda se desejar -->
    <stroke
        android:width="1dp"
        android:color="@color/white"/> <!-- Cor da borda branca -->
</shape>
